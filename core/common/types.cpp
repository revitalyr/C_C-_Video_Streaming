module;

// Global module fragment for standard library headers
#include <cstdint>
#include <string>
#include <vector>

module video_streaming.common.types;

// Empty implementation file for the common library
// This file exists to ensure the library builds properly
