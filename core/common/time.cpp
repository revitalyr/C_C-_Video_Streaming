module;

// Include any necessary non-module headers in the global fragment
#include <chrono>

module video_streaming.common.time;

import video_streaming.common.types;

// Empty implementation file for the time utilities
// This file exists to ensure the library builds properly
